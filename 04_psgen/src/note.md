# these are all depencies use in useCallback

(length,
numberAllowed,
charAllowed,
setpassword,)

# onChange={() => {

# setnumberAllowed((prev) => !prev); }}

// useref hook -- reference le sakte ho kisi bhi element ka

// useEffect
useEffect(() => {
PasswordGenerator();
}, [length, numberAllowed, charAllowed, PasswordGenerator]);
